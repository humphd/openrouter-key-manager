jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/prepare
      - run: pnpm typecheck

name: Typecheck

on:
  pull_request: ~
  push:
    branches:
      - main
